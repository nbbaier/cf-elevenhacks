# Project: cf-elevenhacks

## Package Manager

Use `bun` instead of `npm`/`npx` for all commands (install, run, tsc, etc.).

## Type Checking

```
bun tsc --noEmit
```

## Dev Server

```
bun run dev
```
